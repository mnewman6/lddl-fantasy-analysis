# Streamlit Cloud secrets template.
#
# On SCC: paste these key=value lines into the app's "Secrets" panel
# (Manage app → Settings → Secrets). Do NOT commit a real secrets.toml.
#
# Locally, copy this file to .streamlit/secrets.toml if you ever want to
# override .env values for the running dashboard — but the .env path is
# usually simpler for local dev.

SLEEPER_LEAGUE_ID = "1312102968723505152"
LEAGUE_NAME = "LDDL"
